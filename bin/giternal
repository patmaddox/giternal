#!/usr/bin/env ruby
require "rubygems"
require "giternal"

action = ARGV[0]
available_actions = %w(update freeze unfreeze)
unless available_actions.include?(action)
  puts "Usage: grl (#{available_actions.join(':')})"
  exit 1
end

app = Giternal::App.new(FileUtils.pwd)
app.send(action)
